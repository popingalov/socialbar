interface IHeaderIconTypes {
  readonly burgerMenu: string;
  readonly backArrow: string;
  readonly searching: string;
  readonly extraMenu: string;
}

export const headerIconTypes: IHeaderIconTypes = {
  burgerMenu: 'burgerMenu',
  backArrow: 'backArrow',
  searching: 'searching',
  extraMenu: 'extraMenu',
};
