import styled from 'styled-components';

export const Wrapper = styled.button`
  background-color: transparent;
  border: none;
  color: #fff;
  font-size: 18px;

  &:hover {
    cursor: pointer;
  }
`;
